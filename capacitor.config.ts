import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DarkStore.V.2.0',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
